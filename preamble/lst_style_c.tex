\usepackage{xcolor}
\usepackage{listings}

% Define VSCode Light+ colors.
\definecolor{vsForeground}{HTML}{000000}
\definecolor{vsKeyword}{HTML}{0000FF}
\definecolor{vsType}{HTML}{267F99}       % FILE, uints, etc.
\definecolor{vsString}{HTML}{A31515}
\definecolor{vsComment}{HTML}{008000}
\definecolor{vsControl}{HTML}{AF00DB}    % if, else, etc.
\definecolor{vsFunction}{HTML}{A57D02}

% Define style for C language.
\lstdefinestyle{myCStyle}{
    language    = C,
    aboveskip   = 5pt,
    breaklines  = true,
    numbers     = left,
    numbersep   = 5pt,     % Placement in
    xleftmargin = 10pt,    % solution box.
    basicstyle   = \ttfamily\small\color{vsForeground},
    numberstyle  = \tiny\color{gray},
    commentstyle = \color{vsComment}\itshape,
    stringstyle      = \color{vsString},
    showstringspaces = false,
    keywordstyle = [1]\color{vsKeyword},
    morekeywords = [1]{%
        int, double, char, bool, void, sizeof, snprintf, 
        TRIALS, ALPHA_SIZE, CIPHER_SIZE, 
        MASK_LOW_28, MASK_LOW_32
        },
    emphstyle = [1]\color{vsControl},
    emph      = [1]{if, else, for, while, break, continue, return},
    emphstyle = [2]\color{vsFunction},
    emph      = [2]{%
        fopen, fclose, fread, fscanf, pow, ceil, isalpha, 
        main, malloc, memset, free, printf, 
        readCipher, getAverageIOCs, setFrequency, getIOC, 
        getBestKeySize, setEngFrequency, setCaesarShift, 
        getChiSq, getChiMin, getDecrypt, 
        applyTransform, leftShift, applySBox
        },
    emphstyle = [3]\color{vsType},
    emph      = [3]{FILE, uint8_t, uint32_t, uint64_t}
}

% Define style for Python language.
\lstdefinestyle{myPyStyle}{
    language    = Python,
    aboveskip   = 5pt,
    breaklines  = true,
    numbers     = left,
    numbersep   = 5pt,     % Placement in
    xleftmargin = 10pt,    % solution box.
    basicstyle   = \ttfamily\small\color{vsForeground},
    numberstyle  = \tiny\color{gray},
    commentstyle = \color{vsComment}\itshape,
    stringstyle      = \color{vsString},
    showstringspaces = false,
    keywordstyle = [1]\color{vsKeyword},
    morekeywords = [1]{INF},
    emphstyle = [1]\color{vsControl},
    emph      = [1]{if, else, elif, return},
    emphstyle = [2]\color{vsFunction},
    emph      = [2]{print, pow, slope, ec_sum},
}